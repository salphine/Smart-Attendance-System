# Core dependencies
streamlit==1.28.1
numpy==1.24.3
pandas==2.0.3
plotly==5.17.0
opencv-python-headless==4.8.1.78
pillow==10.1.0

# These are needed by streamlit but we'll let it handle them
# No need to specify rich, markdown-it-py, etc.
